import { ReplaceFirstSpaceWithBrPipe } from './replace-first-space-with-br.pipe';

describe('ReplaceFirstSpaceWithBrPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceFirstSpaceWithBrPipe();
    expect(pipe).toBeTruthy();
  });
});
